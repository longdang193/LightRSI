# Profile bundle patch: register the TokenPilot eviction plugin on the Host plane.
# Runs in agent/pre-step. No-op until an estimator is configured and R3/R4 land —
# it attaches to the waterfall and defers; it does not mutate the surface yet.
- insert:
    - id: tokenpilot-dsh
      name: '@lightrsi/deepseek-harness-adapter'
