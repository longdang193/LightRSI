{
  "name": "@lightrsi/runtime-core",
  "version": "0.1.0-beta.1",
  "private": true,
  "lightrsi": {
    "role": "foundation"
  },
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit",
    "test": "pnpm exec node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@lightrsi/artifact-store": "workspace:*",
    "@lightrsi/kernel": "workspace:*"
  }
}
